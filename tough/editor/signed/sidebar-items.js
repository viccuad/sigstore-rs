initSidebarItems({"enum":[["PathExists","`PathExists` allows the user of our copy/link functions to specify what happens when the target is being written to a shared targets directory and the file already exists from another repo."]],"struct":[["SignedDelegatedTargets","A set of signed targets role metadata."],["SignedRepository","A set of signed TUF Repository metadata."],["SignedRole","A signed role, including its serialized form (`buffer`) which is meant to be written to file. The `sha256` and `length` are calculated from this buffer and included in metadata for other roles, which makes it imperative that this buffer is what is written to disk."]]});